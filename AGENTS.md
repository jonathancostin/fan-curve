# Agent instructions

After completed work passes its checks, run `./scripts/install-local.sh` so the
stable app gets the local build and starts with the saved curve after login.
Do not skip this step unless the user says not to update the installed app.
